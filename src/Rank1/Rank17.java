package Rank1;

import java.util.*;
import java.util.stream.Collectors;

public class Rank17 {

  /*  public static void main(String[] args) {
    String s="welcometojava";
    getSmallestAndLargest(s,3);
}
    public static Optional<String> getSmallestAndLargest(String s, int k) {
        Optional<String> smallest;
        Optional<String> largest;
        List<String> splitted=new ArrayList<>(Arrays.asList());

        for (int i = 1; i-1+k <s.length() ; i++) {
            splitted.add(s.substring(i-1, i-1+k));
            splitted.add(s.substring(s.length()-1-k+1));
        }

        smallest= splitted.stream().
                sorted().findFirst();
        largest= splitted.stream().
                sorted(Comparator.comparing(t -> t.toString().charAt(0)).
                        reversed()).findFirst();


        //return smallest + "\n" + largest;
    }
*/    }
